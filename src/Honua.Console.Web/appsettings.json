{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Honua": {
    "Console": {
      "Auth": {
        "//": "Operator authentication (honua-console#233). Default is fail-closed: outside Development, with no Mode and no edge configured, every route requires an operator identity and the only sign-in path is the deployment's edge IdP. See docs/console-authentication.md.",
        "Mode": "",
        "EdgeForwarded": {
          "Enabled": false,
          "UserHeader": "X-Forwarded-User",
          "EmailHeader": "X-Forwarded-Email",
          "AccessTokenHeader": "X-Forwarded-Access-Token",
          "SharedSecret": ""
        }
      }
    }
  }
}
