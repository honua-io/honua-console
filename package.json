{
  "name": "@honua/console",
  "version": "0.1.0",
  "private": true,
  "description": "Honua Console: unified web surface for Studio, Catalog, Operate, and Share.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build:metadata": "node scripts/write-build-metadata.mjs",
    "test": "node --test scripts/__tests__/*.test.mjs smoke/parity/__tests__/*.test.mjs",
    "smoke:parity": "node smoke/parity/run.mjs",
    "smoke:workflow": "node smoke/parity/workflow.mjs",
    "smoke:matrix": "node smoke/parity/scenario-matrix.mjs",
    "smoke:parity:test": "node --test smoke/parity/__tests__/*.test.mjs"
  }
}
